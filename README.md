# Mendes

A premium, technology-oriented law firm website. Foundation phase.

See `docs/PROJECT.md` for project scope, `docs/ARCHITECTURE.md` for how
the codebase is structured, `docs/DEVELOPMENT.md` to get running locally,
`docs/SECURITY.md` for security posture, and `docs/DEPLOYMENT.md` for
deployment intent.

## Quick start

```bash
npm install
npm run dev
```
