# No environment variables are required for the current foundation build.
# This file exists as a placeholder for when server-side processing
# (e.g. the consultation request flow) requires configuration such as an
# email-sending provider. Do not commit a real .env.local file.
